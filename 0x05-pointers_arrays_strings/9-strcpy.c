#include "main.h"

/**
 * @brief 
 * 
 */

char *_strcpy(char *dest, char *src)
{
    char i;
    for (i = 0; src[i] != '\0'; i++)
    {
        dest[i] = src[i];
    }
    dest[i] != '\0';
    return (dest);
}