#include <stdio.h>

/**
 * main - print all single digit numbers of base 10
 * starting from 0.
 *
 * Return: always 0
 */

int main(void)
{
for (int num = 0; num <= 9; num++)
{
printf("%d", num);
}
printf("\n");
return (0);
}
